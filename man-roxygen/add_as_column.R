#' @param add_as_column A logic value to indicate if the converted values are to be added as a column to the input dataframe.
#' This option is ignored if \code{x.name} is not a dataframe. The column name will be \code{newobj}. Defaults to \code{TRUE}.
